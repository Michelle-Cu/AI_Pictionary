services:
  app:
    build: .
    ports:
      - "10060:8000"
    env_file:
      - .env
    volumes:
      - game_data:/app/data
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  game_data:
  hf_cache:
