services:
  app:
    build: .
    ports:
      - "10060:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - hf_cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  hf_cache: